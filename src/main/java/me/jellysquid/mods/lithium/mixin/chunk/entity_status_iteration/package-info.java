@MixinConfigOption(description = "Iterate a copy of a section's entities while its tracking status changes, so callbacks that discard entities cannot break the traversal")
package me.jellysquid.mods.lithium.mixin.chunk.entity_status_iteration;

import net.caffeinemc.gradle.MixinConfigOption;
